// --------------------------------------------------------------
// engine.civet – shared BuildEngine for build & watch pipelines 
// decides skip-vs-compile, delegates to pool, then calls writeOutputAndGetResult
// TEST: Watch mode is working!
// --------------------------------------------------------------

fs from "fs-extra"
path from "node:path"
{ WorkerPool } from "../worker/pool.civet"

// Import helpers and types from the refactored modules.
import { resolveOutputType, makeSignature, fileToOutFile, toPosix, logDebug, normalizeSourceMap, isJsExtension } from "../support/utils.civet"
import type { BuildContext } from "../support/config.civet"

{ compile } from "@danielx/civet"
import { rewriteCivetImports } from "../support/import-rewriter.civet"
import type { CompileResult, CivetCompileResult, CompileStateUpdate } from "../types.civet"

export class BuildEngine
    ctx: BuildContext
    pool: WorkerPool
    // Simple in-memory cache to avoid repeated disk reads during watch mode
    contentCache: Map<string, string>
    inFlight: Map<string, Promise<CompileResult>>

    constructor(ctx: BuildContext, workerScriptPath: string)
        @ctx = ctx
        concurrency := Math.max(1, (@ctx.opts.concurrency ?? 2))
        @pool = new WorkerPool(workerScriptPath, concurrency)
        @inFlight = new Map<string, Promise<CompileResult>>()
        @contentCache = new Map<string, string>()

    // Build / rebuild *all* sources currently in ctx.sources
    async buildAll(onProgress?: (result: CompileResult) => void): Promise<CompileResult[]>
        tasks := @ctx.sources.map (file) => @build(file, onProgress)
        results := await Promise.allSettled(tasks)
        // Flatten settled results, handling potential top-level errors from build()
        compiledResults: CompileResult[] := []
        for settledResult of results
            if settledResult.status === 'fulfilled'
                compiledResults.push(settledResult.value)
            else
                compiledResults.push({ status: 'error', file: 'unknown', error: settledResult.reason, outFile: '', signature: '' })
        return compiledResults


    // Build a single file. Dedupe if already in progress.
    build(file: string, onProgress?: (result: CompileResult) => void)
        existingTask := @inFlight.get(file)
        if existingTask
            return existingTask
        

        task := @runCompileTask(file).then (result: CompileResult) =>
            onProgress?.(result)
            return result
        .catch (error: unknown) =>
            err := error instanceof Error ? error : new Error(String(error))
            result: CompileResult := { file, status: 'error', error: err, outFile: '', signature: '' }
            onProgress?.(result)
            return result

        // Track promise for deduplication.
        @inFlight.set(file, task)
        // Clean up once finished.
        task.finally => @inFlight.delete(file)
        return task

    async runCompileTask(file: string): Promise<CompileResult>
        // file might be relative (from glob) or absolute (from watcher), so resolve it first
        fileAbs := path.isAbsolute(file) ? file : path.resolve(@ctx.cwd, file)
        isTsx := resolveOutputType(@ctx, fileAbs)
        // Reuse cached file content when available to avoid IO on rapid rebuilds
        content := if @contentCache.has(fileAbs)
            cachedContent := @contentCache.get(fileAbs)
            if cachedContent
                cachedContent
            else 
                // This should never happen if has() returned true, but defensive code is good
                readData := await fs.readFile(fileAbs, "utf8")
                @contentCache.set(fileAbs, readData)
                readData
            
        else
            readData := await fs.readFile(fileAbs, "utf8")
            @contentCache.set(fileAbs, readData)
            readData
        
        // Determine if output is a JS extension and set js flag accordingly
        // Priority: 1. Explicit file extension 2. Config file js flag 3. Folder-based rules (for generic .civet)
        expectedOut := fileToOutFile(fileAbs, isTsx)
        
        // Check explicit file extension first (highest priority)
        isJsFromExtension := isJsExtension(fileAbs, isTsx)
        
        // Check config file for js flag (from civetconfig.json parseOptions.js)
        parseOpts := @ctx.parseOpts
        isJsFromConfig := parseOpts?.['js'] === true
        
        // Also check if output extension would be JS based on folder rules (for generic .civet files)
        // If folder says output to .js, we should set js: true
        // But explicit extensions always win, so we only check this for generic .civet files
        // Note: Currently, CLI doesn't support outJs/outJsx folder rules, so this is reserved for future
        isJsFromFolder := false
        
        // Combine: explicit extension wins, then config, then folder
        isJs := isJsFromExtension or isJsFromConfig or isJsFromFolder
        
        // If js flag is in parseOpts, we need to extract it and use it as top-level js flag
        // Civet expects js at top level of CompileOptions, not in parseOptions
        // So we'll remove it from parseOpts and use it separately
        cleanParseOpts := if parseOpts
            cleanOpts := { ...parseOpts }
            // Remove js from parseOptions since we handle it at top level
            if cleanOpts['js'] is not undefined
                delete cleanOpts['js']
            cleanOpts
        else
            undefined
        
        signature := makeSignature(content, { tsx: isTsx, inlineMap: @ctx.opts.inlineMap, mapFiles: @ctx.opts.mapFiles, configContent: @ctx.configContent })

        // Skip check (respect --force flag)
        // Normalize keys to POSIX to avoid Windows path separator mismatches in persisted state
        // Use path relative to cwd for consistent keys across different working directories
        fileRel := path.relative(@ctx.cwd, fileAbs)
        key := toPosix(fileRel)
        prev := @ctx.prevHashes[key]
        expectedOutRel := path.relative(@ctx.cwd, expectedOut)
        expectedOutPosix := toPosix(expectedOutRel)
        if !@ctx.opts.force and prev and prev.sig == signature and prev.outFile == expectedOutPosix and await fs.pathExists(expectedOut)
            // Return the skip result - let the caller handle state updates
            logDebug("[COMPILE] Skipping (signature match)", fileAbs)
            return { status: "skip", file: fileAbs, outFile: expectedOut, signature: signature, mapFile: @ctx.opts.mapFiles and await fs.pathExists(expectedOut + ".map") ? expectedOut + ".map" : undefined }

        code .= ""
        mapJson .= null

        // Only generate source maps when the user actually wants them
        wantMap := (@ctx.opts.inlineMap != "none") or @ctx.opts.mapFiles

        // Use worker pool if available and concurrency allows, otherwise compile in-process.
        if (@ctx.opts.concurrency ?? 2) > 1 and @pool.poolAvailable
            try
                logDebug("[COMPILE] Compiling with worker", fileAbs)
                // LIES! The object is still contaminated. Sanitize it right here to be absolutely sure.
                sanitizedParseOpts := if cleanParseOpts then JSON.parse(JSON.stringify(cleanParseOpts)) else undefined
                workerResult := await @pool.exec({ file: fileAbs, content, isTsx, wantMap, js: isJs, parseOpts: sanitizedParseOpts })
                if workerResult.ok
                    code = rewriteCivetImports(workerResult.code)
                    mapJson = workerResult.mapJson
                else
                    throw new Error(workerResult.error)
            catch workerErr
                logDebug("[COMPILE] Worker failed, falling back", workerErr)
                compileOpts := {
                    filename: file,
                    ...(wantMap ? { sourceMap: true } : {}),
                    ...(isJs ? { js: true } : {}),
                    ...(cleanParseOpts ? { parseOptions: cleanParseOpts } : {})
                }
                rawResult := await compile(content, compileOpts)
                compileResult: CivetCompileResult := typeof rawResult === 'string' 
                    ? { code: rawResult } 
                    : rawResult as CivetCompileResult
                code = rewriteCivetImports(compileResult.code)
                if wantMap and compileResult.sourceMap?
                    mapJson = normalizeSourceMap(compileResult.sourceMap.json(file, expectedOut))
        else
            logDebug("[COMPILE] Compiling in-process", fileAbs)
            compileOpts := {
                filename: fileAbs,
                ...(wantMap ? { sourceMap: true } : {}),
                ...(isJs ? { js: true } : {}),
                ...(cleanParseOpts ? { parseOptions: cleanParseOpts } : {})
            }
            try
                safeParse := cleanParseOpts ? JSON.parse(JSON.stringify(cleanParseOpts)) : undefined
                logDebug("[COMPILE_DEBUG] in-process compile options", { wantMap, filename: fileAbs, hasParseOpts: !!cleanParseOpts, parseOpts: safeParse, isJs, isJsFromExtension, isJsFromConfig })
            catch (_err: unknown)
                // Debug logging might fail, ignore silently
                void 0
            rawResult := await compile(content, compileOpts)
            try
                logDebug("[COMPILE_DEBUG] in-process rawResult type", { rawType: typeof rawResult })
            catch (_err: unknown)
                // Debug logging might fail, ignore silently
                void 0
            compileResult: CivetCompileResult := typeof rawResult === 'string' 
                ? { code: rawResult } 
                : rawResult as CivetCompileResult
            code = rewriteCivetImports(compileResult.code)
            try
                logDebug("[COMPILE_DEBUG] in-process compileResult types", { codeType: typeof code, hasSourceMap: !!compileResult.sourceMap })
            catch (_err: unknown)
                // Debug logging might fail, ignore silently
                void 0
            if wantMap and compileResult.sourceMap?
                mapJson = normalizeSourceMap(compileResult.sourceMap.json(fileAbs, expectedOut))

        // ensure cache stores latest content (use absolute path for cache key)
        @contentCache.set(fileAbs, content)

        try
            logDebug("[COMPILE_DEBUG] calling writeOutput", {
                file: fileAbs,
                isTsx,
                codeType: typeof code,
                hasMapJson: !!mapJson,
                mapJsonType: typeof mapJson
            })
        catch (_err: unknown)
            // Debug logging might fail, ignore silently
            void 0
        return await @_writeOutputAndGetResult(fileAbs, isTsx, isJs, code, mapJson, signature)

    // Handles writing the output of a successful compilation to disk.
    // This is now an internal method of the engine.
    async _writeOutputAndGetResult(
        file: string,
        isTsx: boolean,
        isJs: boolean,
        code: string,
        mapJson: string | null,
        signature: string
    ): Promise<CompileResult>
        expectedOut := fileToOutFile(file, isTsx)
        codeOut .= code
        
        // Inject @ts-nocheck header when compiling to JavaScript mode (js: true)
        if isJs
            codeOut = "// @ts-nocheck\n" + codeOut
        
        mapFile := expectedOut + ".map"

        // DEBUG: capture types/flags before writing outputs
        try
            logDebug("[WRITE_DEBUG] about to write outputs", {
                expectedOut,
                codeType: typeof codeOut,
                hasMapJson: !!mapJson,
                mapJsonType: typeof mapJson,
                inlineMap: @ctx.opts.inlineMap,
                mapFiles: @ctx.opts.mapFiles
            })
        catch (_err: unknown)
            // Debug logging might fail, ignore silently
            void 0

        if @ctx.opts.inlineMap is "full" and mapJson
            base64Map := Buffer.from(mapJson).toString("base64")
            codeOut += "\n//# sourceMappingURL=data:application/json;base64," + base64Map + "\n"

        if @ctx.opts.mapFiles and mapJson
            await fs.writeFile(mapFile, mapJson)

            if @ctx.opts.inlineMap is "fileurl"
                codeOut += "\n//# sourceMappingURL=" + path.basename(mapFile) + "\n"
        
        await fs.writeFile(expectedOut, codeOut)

        // --- Cleanup: remove bygone sibling (.ts vs .tsx) to prevent stale duplicates (only for generic .civet files) ---
        // This regex ensures we only do this for files ending in `.civet`, not `.<ext>.civet`
        if (/(?<!\.\w+)\.civet$/.test(file))
            siblingOut := file.replace('.civet', isTsx ? '.ts' : '.tsx')
            try
                if siblingOut != expectedOut && await fs.pathExists(siblingOut)
                    await fs.unlink(siblingOut)
                if await fs.pathExists(siblingOut + '.map')
                    await fs.unlink(siblingOut + '.map')
            catch (err: unknown)
                logDebug("[WRITE] Best-effort cleanup of sibling file failed.", { siblingOut, err })

        logDebug("built", file)
        return { status: "built", file, outFile: expectedOut, signature, mapFile: @ctx.opts.mapFiles and mapJson ? mapFile : undefined }

    // Handle a source deletion - returns state update describing what needs to be deleted.
    createRemoveUpdate(file: string): CompileStateUpdate
        // Clear content cache (safe, internal state)
        @contentCache.delete(file)
        
        // Describe which files to delete based on filename convention
        filesToDelete: string[] := []
        // Regex to find files like `foo.ts.civet`, `bar.cjs.civet`, etc.
        match := file.match(/(.*)(\.\w+\.civet)$/)
        if match && match[1] && match[2]
            out := file.slice(0, -'.civet'.length)
            filesToDelete.push(out, `${out}.map`)
        else // Generic .civet, might be .ts or .tsx
            tsFile := file.replace(".civet", ".ts")
            tsxFile := file.replace(".civet", ".tsx")
            filesToDelete.push(tsFile, tsxFile, `${tsFile}.map`, `${tsxFile}.map`)
        
        return {
            outFiles: [],
            newHashes: {},
            filesToDelete: filesToDelete,
            hashesToDelete: [file]
        }